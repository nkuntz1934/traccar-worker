{
  "name": "traccar-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "bash build-frontend.sh && wrangler d1 migrations apply DB --remote && wrangler deploy",
    "build:frontend": "bash build-frontend.sh"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.14.0"
  }
}
